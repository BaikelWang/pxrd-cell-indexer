# 2026-07-06 — 任务初始化与文档约定

## 做了什么

1. 创建任务目录 `PRXD-Cell-indexing-model-0706`，按 Agent 七步工作流搭好脚手架（docs/、src/、tests/、Makefile）。
2. PM 确认核心目标：**训练 PXRD → 晶系 + lattice 模型**；训练数据 `alex_aflow_oqmd_mp`；benchmark `MP-100samples-benchmark`。
3. 将 PM 确认项写入 `docs/00-requirements.md`、`data/README.md`、`docs/04-progress.md`。
4. 修复 `make test`（ruff / mypy / pytest 全部通过）。
5. 归档旧项目至 `archive/`（Mix2Phase-QPA、RealPXRD-Solver、Uni-3DAR-study）。

## 文档约定（PM 确认，务必遵守）

| 目录 | 用途 |
|---|---|
| **`docs/开发日志/`** | 工作性质：做了什么、优化、设计方案、决策、周报。**随手记录。** |
| **`docs/实验记录/`** | 实验性质：各次实验的设置、过程、结果、分析。**每次实验后留档。** |

已写入 `AGENT.md` 与各目录 `README.md`。

## 下一步

- Step 2 方案设计（模型架构、数据管线、损失函数）
- 首次 baseline 实验 → 记入 `docs/实验记录/`
