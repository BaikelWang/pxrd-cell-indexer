# 2026-07-15 — A0 评测协议护栏

## 交付

| 文件 | 说明 |
|---|---|
| `scripts/a0_protocol_eval.py` | dual-route raw Top-1；ckpt 继承 representation/Niggli/stats |
| `results/a0/r10_slim_valid1400_a0_canonical.json` | valid1400 规范基准（含 per_sample） |
| `results/a0/r10_slim_mp100_a0_canonical.json` | MP100 Niggli raw 里程碑 |
| `results/a0/a0_summary.json` | 摘要 |

代码侧：`eval.py` 增加 classifier vs lattice-inferred CS、cs_correct subset、峰数分层、六参数达标率、A0 metrics block；trainer valid 双路 oracle/predicted；checkpoint 写入 `canonical_convention` / `representation` / `lattice_stats`；`eval_valid` / `eval_mp100` 继承 ckpt 协议。

## R10-slim 复现（strict 0.05 / 3°，rerank=none）

| 指标 | 值 | vs 方案预期 |
|---|---:|---|
| strict raw elem | **21.29%** | ✓ |
| classifier CS | **80.43%** | ✓ ~80.4% |
| lattice-inferred CS | 19.64% | （与 classifier 分离） |
| oracle-CS elem | **21.79%** | ✓ |
| route gap | **0.50 pp** | ✓ <1pp |
| CS-correct subset elem | **26.02%** | ✓ ~26% |
| reproduce_ok（连跑两次） | True | ✓ |

MP100（Niggli，raw Top-1）：strict elem **12.00%**，classifier CS 53%。

## 命令

```bash
python scripts/a0_protocol_eval.py \
  --config configs/scale_100k_r10_slim_film_niggli.yaml \
  --checkpoint results/experiments/scale_100k_r10_slim_film_niggli_seed42/checkpoints/best.pt \
  --convention niggli --ltol 0.05 --atol-deg 3.0
```

## 裁决

A0 通过；可进入 A1-M1（E1c@100k）。跳过 A1-M0 复训。
