# B1-S2 P0.2：time_budget × match_fraction 对照（2026-07-23）

> 生产搜索未改；仅 per-arm 覆盖 kwargs。  
> 脚本：`scripts/diag_b1s2_p02_budget_mf.py`  
> 产物：`results/beat_engine/b1_search/diag_b1s2_p02_budget_mf.json`  
> 子集：consistent ∧ axial_ok=3；每标签 15 样，共 30（其中 24 样对角落在生产轴向窗内）。

---

## 1. 2×2 总表（recall@20）

| 臂 | mf | time | recall@20 | empty | 相对 A |
|----|---:|-----:|----------:|------:|--------|
| **A**（近生产） | 0.95 | ×1 | **43.3%** | 46.7% | — |
| **B** | 0.80 | ×1 | 43.3% | 43.3% | **+0pp** |
| **C** | 0.95 | ×2 | 46.7% | 43.3% | **+3.3pp** |
| **D** | 0.80 | ×2 | 46.7% | 40.0% | **+3.3pp** |

分晶系（臂 A）：ortho **60%** / mono **27%**。加预算主要救了少量 mono。

---

## 2. 残余子集（A 下 empty ∧ 对角已在窗内）

n = **8**

| 臂 | recall@20 | empty |
|----|----------:|------:|
| A | 0% | 100% |
| B（松 mf） | 0% | 87.5% |
| C（×2 预算） | **12.5%**（1/8） | 87.5% |
| D（两者） | **12.5%** | 75% |

→ 松阈值几乎无效；加倍预算只救出 **1/8**。

---

## 3. 结论（auto_hint）

**`logic/seed-limited (kwargs won't save)`**

- 调 `match_fraction` / `time_budget` **不是**最好子集空池的主杠杆（与 P0 放开轴向窗同级：都是 ~0–3pp）。  
- 空池时真胞仍在峰里；对角也多半在种子选项里——问题在 **顺序求解的组合/过滤/枚举顺序**，不是「再给一点时间和更松的门」。

---

## 4. 下一刀（仍先测，再改）

**P0.3 单样种子跟踪**（对象：上述 residual 8 样，或再扩到 empty∧diags_in_window）：

1. 真 (G11,G22,G33) 是否出现在 `g11/22/33_opts`？  
2. 三者是否在同一 `product` 迭代里被枚举到（distinct peak 约束）？  
3. 合成的 ortho/mono gvec 是否过 SPD / `gstar_to_lattice_params`？  
4. `_approx_match_counts` / `_fast_match_count` 对真 gvec 的匹配数是否 ≥ `min_matched`？  
5. 若真种子从未被访问：是排序太靠后还是 `max_diag_seeds` / 早停截断？

根据卡点再改代码（种子排序、去重、distinct-peak 规则、或非轴向对角）——**一次只改一类**。

心型/Niggli 变体仍留给 **mismatch 大盘**（假设 A），对齐 P0.3 修「一致子集逻辑」之后或并行开独立探针。
