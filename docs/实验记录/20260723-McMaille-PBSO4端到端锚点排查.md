# 2026-07-23 — McMaille 端到端锚点排查（样本 `PBSO4.csv`）

> **目的**：用项目内连续谱 `data/PBSO4.csv` 走通「谱 → 寻峰 → McMaille → 候选」，对照 CIF 真值。  
> **真值**（`data/PbSO4-Wyckoff.cif`）：正交 Pnma，`a=8.48, b=5.398, c=6.958, V≈318.5`  
> **工作目录**：`third_party/McMaille/run_lab/`

---

## 结论

McMaille **找对了**：建议解

`a=8.4845, b=5.4002, c=6.9649, α=β=γ=90°, P Ortho, McM20≈56.7, V≈319.1`

相对 CIF：边长偏差约 **0.05–0.1%**。墙钟 **~68 s**（其中单斜搜索占 **~65 s**）。

---

## Stage P0 — 连续谱 → 峰表（McMaille 外）

| | |
|---|---|
| **输入** | `data/PBSO4.csv`：6010 点，`2θ∈[10,160.2]`，步长 0.025°，连续强度 |
| **处理** | 窗口 `[10,70]`；Savitzky–Golay(11,3)；`find_peaks`（height/prominence）；按 2θ 升序取前 20 |
| **产物** | `run_lab/pbso4b.dat`（黑盒 `NGRID=-3`）；`pbso4_peak_audit.json` |
| **λ / zero** | `1.54056` / `0.0`（假设 CuKα；CSV 未标注） |
| **耗时** | <1 s |

前几强峰（节选）：20.80 / 23.30 / 26.70 / 29.68 / 43.73° …

---

## Stage A00–A11 — McMaille（本次实测）

| 锚点 | 阶段 | 输入 | 产物 | ΔCPU | 累计 |
|---|---|---|---|---:|---:|
| **A00** | 启动 | `pbso4b.dat` + `*.hkl` | 打开 `pbso4b.imp` | 0.000 | 0.000 |
| **A01** | 峰解析+伪谱 | 20 峰 | `W=0.30`, `Nind=3`, `Rmin=0.02`；`pbso4b-new.dat` | 0.000 | 0.000 |
| **A02** | 进入搜索 | 伪谱 | 开始 MC | 0.000 | 0.000 |
| **A03** | 立方 | `cub.hkl` | 无可信解 | **0.003 s** | 0.004 |
| **A04** | 六方/菱方 | `hex/rho.hkl` | 无 | **0.650 s** | 0.653 |
| **A05** | 四方 | `tet.hkl` | 无 | **1.509 s** | 2.162 |
| **A06** | **正交** | `ort.hkl` | **命中真解**：`8.485×5.400×6.965, Rp≈0.053` | **0.464 s** | 2.627 |
| **→ mono** | 单斜（未单独打 A07） | `mon.hkl` | 出现近正交伪解 `β≈90.08°, Rp≈0.032` | **65.21 s** | 67.84 |
| **A08** | 搜索结束 | 候选池 | 进入 FoM | — | 67.84 |
| **A09** | McM20 排序 | 全候选 | `.ckm` / `.mcm` | ~0 | 67.84 |
| **A10** | Best + CELREF | Top McM20 | 精修 `a=8.4866, b=5.4000, c=6.9654`；`M20=27.3, F20=27.2`；`.prf` | 0.001 | 67.84 |
| **A11** | 结束 | — | 总 CPU **67.84 s**（墙钟 ~67.9 s） | — | 67.84 |

> **耗时结构**：单斜占 **~96%**；真解在正交段 **0.5 s 内**已找到，但黑盒仍会把单斜跑完（本次未触发 `Rp < Rmin` 提前停：正交最佳 Rp≈0.053 > Rmin=0.02）。

---

## 与真值对照

| | a | b | c | V | 晶系 |
|---|---:|---:|---:|---:|---|
| CIF | 8.480 | 5.398 | 6.958 | 318.5 | Ortho Pnma |
| McMaille 建议（McM20） | 8.4845 | 5.4002 | 6.9649 | 319.1 | P Ortho |
| CELREF 精修 | 8.4866 | 5.4000 | 6.9654 | 319.2 | — |

---

## 产物文件

| 文件 | 说明 |
|---|---|
| `pbso4b.dat` | 寻峰后的 McMaille 输入 |
| `pbso4_peak_audit.json` | 寻峰参数与峰列表审计 |
| `pbso4b.imp` | 主日志（含 `>>> ANCHOR`） |
| `pbso4b.ckm` / `.mcm` / `.prf` | 候选 / CRYSFIRE / 伪谱 |
| `pbso4b_console.log` | 屏幕回放 |
| `pbso4b_ort.ckm` / `pbso4b_mon.ckm` | 分晶系候选 |

复跑：

```bash
cd third_party/McMaille/run_lab
# 若需重寻峰：重新生成 pbso4b.dat 后
./mcmaille_anchored pbso4b | tee pbso4b_console.log
rg '>>> ANCHOR' pbso4b.imp
```

---

## 相对 `y2o3b` 的差异（排查要点）

| | y2o3b | PBSO4 |
|---|---|---|
| 输入形态 | 现成峰表 | **连续谱 CSV → 必须先寻峰** |
| 真解晶系 | 立方 | **正交** |
| 真解首次出现 | A03 立方 ~0.01 s | **A06 正交 ~0.5 s** |
| 是否提前停 | 正交段 `Rp<Rmin` 跳过单斜 | **不提前停**，单斜仍跑 ~65 s |
| Top-1 | Cubic I | **Ortho P**（正确） |
