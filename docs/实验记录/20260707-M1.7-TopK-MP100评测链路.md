# 2026-07-07 — M1.7 Top-K + 真实 lattice match + MP100 评测链路

> **性质**：打通评测链路（plumbing 验证），非正式 benchmark 结论  
> **权重**：`smoke_unfrozen_seed42` best.pt（5 epoch，10k 训练）  
> **主指标**：pymatgen `Lattice.find_mapping`（ltol=0.3, atol=10°），**纯晶格、非原子 StructureMatcher**

---

## 1. 实现摘要

| 模块 | 路径 | 职责 |
|---|---|---|
| Top-K 生成 | `src/pxrd_cell_indexing/model/topk.py` | D26：主候选 + 晶系变体 + 倍胞/子胞变换 → K=20 |
| 真实 lattice match | `src/pxrd_cell_indexing/eval.py` | `top1_lattice_match_rate` / `topk_lattice_match_rate` |
| MP100 模拟 | `src/pxrd_cell_indexing/data/mp100.py` | CIF → 与训练一致口径的峰表 + primitive truth |
| valid 评测 | `scripts/eval_valid.py` | valid1400 上 proxy vs pymatgen 交叉验证 |
| MP100 评测 | `scripts/eval_mp100.py` | 100 CIF 端到端 + Mc/JADE 参照表 |

---

## 2. valid1400（in-distribution 参照）

配置：`smoke_unfrozen_seed42`，K=20  
产出：[`results/valid1400_real_match_smoke_unfrozen_seed42.json`](../../results/valid1400_real_match_smoke_unfrozen_seed42.json)

| 指标 | proxy（训练监控） | pymatgen 真实 |
|---|---:|---:|
| Top-1 lattice match | 16.8% | **32.4%** |
| Top-20 lattice match | — | **99.1%** |
| 晶系准确率 | — | 53.4% |

**交叉验证结论**：proxy **偏保守**（低于 pymatgen 真实 Top-1），数量级合理，未虚高。

---

## 3. MP100（out-of-distribution 对照）

配置：同上，100 CIF 全部成功模拟  
产出：[`results/mp100_eval_smoke_unfrozen_seed42.json`](../../results/mp100_eval_smoke_unfrozen_seed42.json)

| 指标 | smoke unfrozen 5ep | 参照基线（ideal 峰） |
|---|---:|---:|
| Top-1 lattice match | **47%** | McMaille **76.4%** / JADE9 **72.5%** |
| Top-20 lattice match | **100%** | — |
| RealPXRD Without L | — | **5%**（任务错配，不可比） |

**解读**：
- Top-1 **47%** 在 5 epoch smoke 权重下已显示学习信号，但距 Mc/JADE 仍有明显差距（预期，待 M2 全量训练）。
- Top-20 **100%** 被倍胞/子胞启发式变体显著抬高（`find_mapping` 对倍胞等价敏感）；**正式对比以 Top-1 为主**。

---

## 4. 结论与下一步

- **链路验证通过**：CIF 模拟 → 模型推理 → Top-K → pymatgen lattice match → JSON 落盘，端到端无阻塞。
- **指标口径明确**：indexing 用 `Lattice.find_mapping`；不做原子级 StructureMatcher。
- **下一步（M2）**：全量 241113 训练后重跑 MP100，以 Top-1 lattice match 与 Mc/JADE 正式对照。
