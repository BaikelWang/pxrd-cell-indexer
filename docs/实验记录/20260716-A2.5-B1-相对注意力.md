# A2.5-B1 · 物理相对注意力 A-adv — Gate 失败 / 淘汰

日期：2026-07-16～17  
状态：**P0 Gate FAIL → 不跑 100k；臂淘汰**（代码保留，默认 `rel_attn=none`）  
基线：**A2-ctrl**（cs_conditional L=6，max_peaks=48，strict **40.57%**）  
Gate：overfit700 上 `valid/strict_raw_top1_elementwise_rate` ≥ **95%**

## 实现（已合入，默认关闭）

- `b_ij = φ(g_i − g_j)` 注入 Transformer additive attn mask
- CLS 行/列恒为 0；pad 并入 float mask（`-inf`）
- `scalar` / `mlp`；零初始化
- config：`peak_transformer_rel_attn ∈ {none,scalar,mlp}`、`peak_transformer_rel_freqs`
- 单测：`tests/test_peak_transformer.py`（7/7）
- 训练恢复：`scripts/train.py --resume`

## P0 结果

| 臂 | rel_attn | last ep | best strict | Gate | 100k |
|----|----------|--------:|------------:|------|------|
| B1a | scalar | 1200 | **3.43%** @ep48 | **FAIL** | **跳过** |
| B1b | mlp | 1093（提前停） | **4.71%** @ep915 | **FAIL** | **跳过** |

对照：T3 Fourier P0 约 ep756 即达 ≥95%。本臂到 ep1100+ 仍 <5%，说明当前 rel-attn 注入破坏了过拟合能力（非「还没训够」）。

## 裁决

- **淘汰 B1 100k**；不再占用 A2.5 批次预算。
- 若日后重开：先修实现/数值路径并重过 P0，再谈 100k。
- 2026-07-17：已停 B1 P0，显存留给 B2a∥B2b。

## 日志

- `results/beat_engine/a2_a2p5/b1a_p0_rel_scalar.log`
- `results/beat_engine/a2_a2p5/b1b_p0_rel_mlp.log`
- `results/experiments/overfit700_a2p5_b1a_rel_scalar_seed42/`
- `results/experiments/overfit700_a2p5_b1b_rel_mlp_seed42/`
