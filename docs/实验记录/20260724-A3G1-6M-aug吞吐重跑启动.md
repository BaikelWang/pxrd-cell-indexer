# A3-G1 × 6M 重跑：RealPXRD 加噪 + 吞吐调参（2026-07-24）

## 动机

首轮干净谱 6M（`scale_6m_a3_g1_gstar6_seed42`）在 **ep14** 达到 valid `strict_raw_top1_elementwise_rate = 48.0%`（相对 100k 42.5% **+5.5 pp**），触发「远超」Gate。按计划停掉首轮，用更完整配置重跑。

## 相对首轮变更

| 项 | 首轮 | 本轮 |
|----|------|------|
| `experiment_name` | `scale_6m_a3_g1_gstar6_seed42` | `scale_6m_a3_g1_gstar6_aug_bs512_seed42` |
| `train_augment` | false | **true** |
| `augment_shift_range` | 0.0 | **0.1**（噪声/scale 用 `SpectrumAugmentConfig` 默认 0.05 / 0.8–1.2） |
| `valid_augment` | false | false |
| `batch_size` | 256 | **512** |
| `num_workers` | 8 | **12** |
| `prefetch_factor` | 2（默认） | **4** |
| 日程 | max 20 / patience 5 | 同左 |

配置：`configs/scale_6m_a3_g1_gstar6_aug.yaml`  
启动：`scripts/run_scale_6m_a3_g1_aug.sh`  
日志：`logs/scale_6m_a3_g1_gstar6_aug_bs512_seed42.train.log`

## 对照

- valid1400 仍干净，便于和首轮 / 100k 比 early-stop 曲线。
- 训练结束后另跑 clean + `--xrd-augment` MP100。

## 启动

- 已停首轮 `train.py --config configs/scale_6m_a3_g1_gstar6.yaml`（保留其 `best.pt` / metrics 至 ep14）。
- 本轮于 2026-07-24 07:00Z 启动。

## 吞吐观察（启动后 ~5 min）

- `nvidia-smi`：GPU util 仍约 **16–28%**，显存 **~2.8 / 24 GiB**，功耗 ~80 W —— 与首轮同量级，仍偏 DataLoader/LMDB+online augment CPU 瓶颈。
- `batch_size=512` 已翻倍；VRAM 仍很空，若后续要再挤吞吐可试 768/1024（OOM 再降）。
- 12 个 DataLoader worker 在跑；训练进程 CPU ~100%。
